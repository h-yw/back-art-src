# BackArt 项目文档

## 1. 项目概述

`BackArt` 是一个使用 Flutter 开发的跨平台壁纸制作工具。其核心愿景是成为一款轻量、强大且富有创造力的应用，允许用户通过简单的操作，将文字、色彩和图形组合成具有艺术感的个性化手机壁纸。项目旨在提供极致的用户体验和优雅的设计，让创造本身成为一种乐趣。

项目利用 Flutter 的跨平台能力，旨在覆盖 Android, iOS, Web, macOS, Windows, 和 Linux 等主流平台，为不同设备的用户提供一致的体验。

## 2. 核心功能

根据现有文件，`BackArt` 当前具备和规划的功能如下：

*   **文本壁纸生成**: 用户输入任意文本，应用可以将其转换为艺术壁纸。
*   **图片转字符画 (规划中)**: 用户选择设备上的图片，将其转换为字符画（ASCII Art）。
*   **高度自定义**:
    *   **字符集**: 支持使用自定义的字符序列来渲染图像（针对字符画功能）。
    *   **字体支持**: 内置多种艺术字体，并允许用户选择，以实现不同的渲染风格。
    *   **颜色调整**: 支持调整壁纸的前景色和背景色。
    *   **排版布局**: 支持调整文本或字符画在画布上的对齐方式。
*   **导出与分享**: 支持将生成的作品导出为图片，并通过系统分享功能分享给他人。
*   **本地化**: 支持中文和英文两种语言。

## 3. 技术栈

*   **框架**: Flutter
*   **语言**: Dart
*   **状态管理**: 目前项目主要使用 `setState` 进行状态管理，但 `DEVELOPMENT_PLAN.md` 中已明确提出将引入 **Riverpod** 作为主要的状态管理框架。
*   **核心依赖**:
    *   `intl`: 国际化和本地化。
    *   `flutter_riverpod`: (规划中) 用于状态管理。
    *   `image_picker`: 从图库选择图片。
    *   `saver_gallery`: 保存图片到系统相册。
    *   `share_plus`: 跨平台分享功能。
    *   `sqflite` & `path_provider`: 本地数据库存储（如用户设置）。
    *   `flutter_native_splash` & `flutter_launcher_icons`: 定制启动屏和应用图标。
    *   `url_launcher`: 打开外部链接。
    *   `quick_actions`: 移动端桌面快捷操作。
    *   `flutter_colorpicker`: 颜色选择器。
    *   `device_info_plus`: 设备信息。
    *   `flutter_svg`: SVG 渲染。
    *   `emoji_picker_flutter`: 表情选择器。

## 4. 项目结构

项目遵循标准的 Flutter 项目结构，并已开始按功能模块（Feature-First）划分：

```
lib/
├── main.dart
|
├── config/                     # 应用配置，如字体列表、模板
├── core/                       # 核心服务与工具 (数据库、依赖注入、通用工具)
├── db/                         # 数据库管理
├── features/                   # 功能模块 (画布、编辑器、导出、设置等)
├── l10n/                       # 国际化
├── model/                      # 数据模型
├── shared/                     # 跨功能共享的Widget和模型
├── theme/                      # 应用主题和样式
├── utils/                      # 通用工具类
└── widgets/                    # 可复用的自定义组件
```

`assets/` 目录存放静态资源，如字体、图标、图片等。
`l10n-arb/` 存放用于本地化的 `.arb` 文件。
`android/`, `ios/` 等目录存放各个平台特定的代码。

## 5. 项目规划 (来自 DEVELOPMENT_PLAN.md)

### 5.1. 核心问题分析

当前版本存在以下问题：

1.  **状态管理混乱**: 所有画布属性存储在 `Map<String, dynamic>` 中，类型不安全；使用顶层 `StatefulWidget` 和 `setState` 导致频繁重绘，性能开销大。
2.  **UI 与逻辑高度耦合**: `pages/created/created.dart` 文件过于庞大，混合了UI、状态、业务逻辑和交互处理，难以维护和扩展。
3.  **画布实现僵化**: 画布元素一次性绘制，缺乏分层概念，难以实现复杂功能。
4.  **UI/UX 有待提升**: 编辑功能隐藏在底部 `BottomSheet` 中，操作不直观；UI 风格可更现代化。

### 5.2. 重构目标

*   **提升代码质量**: 引入清晰架构，分离UI和业务逻辑，提高可读性、可维护性、可测试性。
*   **优化应用性能**: 采用先进状态管理方案，实现局部刷新，减少重绘。
*   **增强功能扩展性**: 建立灵活的、基于图层的画布系统，为未来功能奠定基础。
*   **革新 UI/UX**: 打造更直观、美观、符合现代设计规范的用户界面。

### 5.3. 技术架构方案

*   **状态管理**: 引入 **Riverpod**，实现声明式、响应式、编译时安全和精细化控制的状态管理。
*   **画布核心重构**: 引入**图层化**概念。
    *   `CanvasState`: 包含图层列表 `List<Layer>` 和全局属性。
    *   `Layer` 基类: 定义通用属性，创建 `TextLayer`, `BackgroundLayer` 等子类。
    *   `CanvasPainter`: 纯粹的绘制器，根据 `CanvasState` 遍历图层并绘制。
*   **新项目结构**: 采用按功能模块（Feature-First）划分的目录结构。

### 5.4. UI/UX 优化方案

*   **主界面重构**: 采用沉浸式布局，顶部画布预览，底部上下文相关**主工具栏**，可设置图层管理面板。
*   **上下文编辑面板**: 选中图层时动态显示专属编辑选项，取代全屏 `BottomSheet`。
*   **设计语言**: 全面拥抱 **Material Design 3 (M3)**，优化色彩、组件和版式。

### 5.5. 开发路线图

*   **第一阶段：基础重构**: 引入 Riverpod，搭建 Provider 体系；创建新项目结构；定义 `Layer` 模型；重构 `CanvasPainter`。
*   **第二阶段：UI/UX 重构**: 构建新主界面布局；拆分编辑功能为独立 Widget；实现上下文编辑面板；全面应用 M3 风格。
*   **第三阶段：功能增强**: 实现图层管理；增加 `ImageLayer` 等新图层类型；引入滤镜；优化导出模块。

## 6. 后续迭代建议

在现有规划的基础上，我提出以下更具体和深入的迭代建议，旨在将 `BackArt` 打造成一款功能全面、体验优秀、具有社区生态的顶级应用。

### 6.1. 阶段一：核心功能完善 (近期)

1.  **实时预览与交互优化**:
    *   **实时渲染**: 在用户调整参数（如字符集、字体、颜色）时，画布内容应实时更新，提供即时反馈。
    *   **性能隔离**: 对于图片转字符画这种计算密集型任务，应将其放到独立的 `Isolate` 中执行，避免 UI 线程卡顿，并提供清晰的加载指示器。
2.  **高级图片处理**:
    *   **图像滤镜**: 在图片转换为字符画之前，允许用户对原始图片应用滤镜（如灰度、二值化、边缘检测），以生成不同风格的字符画。
    *   **选区转换**: 允许用户只转换图片的一部分区域。
3.  **引入强大的状态管理**: 按照 `DEVELOPMENT_PLAN.md` 的规划，优先引入 `Riverpod`，并逐步将现有状态管理迁移到 Riverpod，确保代码结构清晰、可维护性高。

### 6.2. 阶段二：创意功能拓展 (中期)

1.  **动画字符画 (GIF to ASCII)**:
    *   **功能**: 支持用户导入 GIF 动图，并将其转换为字符画动画。
    *   **实现**: 逐帧读取 GIF，将每一帧转换为字符画，然后合成为新的 GIF 或视频文件。这将是一个极具吸引力的“杀手级”功能。
2.  **自定义字体和字符集管理**:
    *   **字体导入**: 允许用户从设备导入自己的 `.ttf` 或 `.otf` 字体文件。
    *   **字符集预设**: 提供多种有趣的字符集预设（如“二进制”、“表情符号”、“像素块”），并允许用户保存和管理自己的常用字符集。
3.  **模板市场**:
    *   **功能**: 创建一个模板库，用户可以将自己满意的参数配置（字体、字符集、颜色等的组合）保存为模板，并应用到其他作品上。
    *   **商业化**: 未来可拓展为用户可分享、下载甚至交易模板的市场。

### 6.3. 阶段三：社区与生态构建 (远期)

1.  **云端服务集成**:
    *   **技术选型**: 推荐使用 `Firebase` 或 `Supabase` 作为后端服务，它们提供了认证、存储、数据库等全套解决方案，能极大加速开发。
    *   **核心功能**: 用户账户系统、云端同步（同步用户的作品、设置和自定义模板）。
2.  **建立作品分享社区**:
    *   **功能**: 在应用内建立一个“发现”或“广场”页面，用户可以将自己的字符画作品发布到社区。
    *   **互动**: 其他用户可以浏览、点赞、评论和收藏作品，甚至“Fork”他人的作品进行二次创作。
3.  **开放 API**:
    *   **目标**: 为其他开发者提供 API，让他们可以在自己的应用或网站中集成 `BackArt` 的字符画生成能力。
    *   **价值**: 扩大 `BackArt` 的品牌影响力，构建开发者生态。

## 7. 总结

`BackArt` 项目有一个非常好的起点和清晰的定位。通过**完善核心功能**、**拓展创意玩法**和**构建社区生态**这三个阶段的迭代，它有潜力从一个优秀的工具应用成长为一个富有活力和创造力的平台。建议团队在开发过程中始终关注**性能**和**用户体验**这两个基本点。