# BackArt 壁纸应用重构与发展计划

## 1. 项目愿景 (Project Vision)

BackArt 旨在成为一款轻量、强大且富有创造力的壁纸制作工具。用户可以通过简单的操作，将文字、色彩和图形组合成具有艺术感的个性化手机壁纸。我们追求极致的用户体验和优雅的设计，让创造本身成为一种乐趣。

## 2. 核心问题分析 (Analysis of Core Issues)

通过对当前 v1.0.0 代码的审查，我们识别出以下核心问题，这些问题限制了应用的可维护性、可扩展性和性能：

1.  **状态管理混乱:**
    *   所有画布属性（文本、颜色、字体、尺寸等）都存储在一个 `Map<String, dynamic>` 中，类型不安全，容易出错。
    *   采用顶层的 `StatefulWidget` 和 `setState` 来管理所有状态，导致任何微小的改动（如调整字号）都会触发整个主界面的重绘，性能开销大。

2.  **UI 与逻辑高度耦合:**
    *   `pages/created/created.dart` 文件过于庞大（超过500行），混合了UI布局、状态更新、业务逻辑（如导出图片）和用户交互处理，违反了单一职责原则。
    *   代码难以阅读、修改和测试。添加新功能（如滤镜、贴图）将使这个文件变得更加臃肿。

3.  **画布实现僵化:**
    *   当前的画布实现将所有元素（背景、文字、水印）一次性绘制，缺乏分层概念。这使得实现更复杂的功能（如图层顺序调整、单个元素的操作）变得异常困难。

4.  **UI/UX 有待提升:**
    *   所有编辑功能都隐藏在底部弹出的 `BottomSheet` 中，操作流程不够直观和高效。
    *   UI风格和组件可以更现代化，遵循 Material Design 3 (M3) 规范可以显著提升应用的视觉质感和用户体验。

## 3. 重构目标 (Refactoring Goals)

本次重构旨在解决上述核心问题，达成以下目标：

*   **提升代码质量:** 引入清晰的架构，分离UI和业务逻辑，提高代码的可读性、可维护性和可测试性。
*   **优化应用性能:** 采用先进的状态管理方案，实现局部刷新，减少不必要的重绘，确保流畅的用户体验。
*   **增强功能扩展性:** 建立一个灵活的、基于图层的画布系统，为未来添加新功能（如图片图层、形状、滤镜等）奠定坚实的基础。
*   **革新 UI/UX:** 打造一个更直观、美观、符合现代设计规范的用户界面，优化核心创作流程。

## 4. 技术架构方案 (Proposed Technical Architecture)

### 4.1. 状态管理 (State Management)

*   **方案:** 引入 **Riverpod** 作为主要的状态管理框架。
*   **理由:**
    *   **声明式与响应式:** 优雅地管理状态的生命周期，自动处理依赖关系。
    *   **编译时安全:** 相比于 Provider，能更早地发现错误。
    *   **精细化控制:** 可以精确地控制哪些Widget需要重绘，完美解决当前 `setState` 导致的性能问题。
    *   **依赖注入:** 解耦业务逻辑服务与UI层，便于测试和复用。

### 4.2. 画布核心重构 (Canvas Core Refactoring)

我们将引入**图层化**的概念来重构画布。

*   **画布状态模型 (`CanvasState`):**
    *   创建一个 `CanvasState` 类，它包含一个图层列表 `List<Layer>` 和画布的全局属性（如尺寸、背景色）。
    *   使用 Riverpod 的 `StateNotifierProvider` 来管理 `CanvasState`。

*   **图层基类 (`Layer`):**
    *   定义一个抽象基类 `Layer`，包含所有图层的通用属性（如ID、位置、旋转、缩放、透明度）。
    *   创建具体的图层子类，例如：
        *   `TextLayer`: 包含文字内容、字体、颜色、对齐方式等。
        *   `BackgroundLayer`: 包含纯色或渐变色背景。
        *   `ImageLayer` (未来扩展): 包含图片资源。

*   **画布绘制器 (`CanvasPainter`):**
    *   `CustomPainter` 的实现将变得非常纯粹。它接收 `CanvasState` 作为输入，遍历 `List<Layer>`，并根据每个图层的类型和属性，调用相应的绘制方法将其绘制到画布上。

### 4.3. 新项目结构 (New Project Structure)

为了支持新的架构，我们将采用按功能模块（Feature-First）划分的目录结构：

```
lib/
├── main.dart
|
├── core/                     # 核心服务与工具
│   ├── database/             # 数据库
│   ├── di/                   # 依赖注入 (Riverpod Providers)
│   └── utils/                # 通用工具类
|
├── features/                 # 功能模块
│   ├── canvas/               # 画布核心
│   │   ├── state/            # CanvasState模型和Provider
│   │   ├── model/            # Layer模型
│   │   └── view/             # Canvas Widget和Painter
│   │
│   ├── editor/               # 编辑器主界面
│   │   ├── view/             # Editor Screen, 工具栏
│   │   └── widgets/          # 各种编辑面板 (Text, Color, etc.)
│   │
│   ├── settings/             # 设置页面
│   │   └── ...
│   └── export/               # 导出功能
│       └── service/          # 导出服务
|
├── shared/                   # 跨功能共享的Widget和模型
│   └── widgets/
|
└── l10n/                     # 国际化
```

## 5. UI/UX 优化方案 (UI/UX Optimization Plan)

### 5.1. 主界面重构

*   **布局:** 采用更沉浸式的布局。顶部为画布预览，底部为可收起的、上下文相关的**主工具栏**。右侧或左侧可以设置一个**图层管理面板**。
*   **主工具栏:** 包含最核心的操作入口：`添加图层` (文本、图片等)、`背景设置`、`导出`。
*   **上下文编辑面板:** 当用户选中一个图层时，主工具栏下方会动态显示该图层的专属编辑选项（例如，选中文本图层，显示字体、颜色、大小等编辑工具），而不是全屏的 `BottomSheet`。

### 5.2. 设计语言

*   全面拥抱 **Material Design 3 (M3)**。
*   **色彩:** 使用 M3 的动态色彩方案或预设的和谐色板。
*   **组件:** 使用 M3 风格的组件 (`FilledButton`, `NavigationBar`, `Slider` 等)。
*   **版式:** 优化字体排版和间距，提升信息的可读性。

## 6. 开发路线图 (Development Roadmap)

### 第一阶段：基础重构 (Phase 1: Foundational Refactoring)

*   **任务1:** 引入 `flutter_riverpod` 依赖，搭建基础的 Provider 体系。
*   **任务2:** 按照新的项目结构创建文件夹。
*   **任务3:** 定义 `Layer` 基类和 `TextLayer`, `BackgroundLayer` 模型。
*   **任务4:** 创建 `CanvasState` 和对应的 `StateNotifierProvider`。
*   **任务5:** 重构 `CanvasPainter`，使其能够根据新的 `CanvasState` 进行绘制。
*   **目标:** 完成核心数据结构和状态管理的迁移，画布可以基于新的模型正确显示。

### 第二阶段：UI/UX 重构 (Phase 2: UI/UX Refactoring)

*   **任务1:** 构建新的主界面布局，包含画布、主工具栏和图层面板。
*   **任务2:** 将旧的编辑功能（文本、颜色等）拆分成独立的、由 Riverpod 驱动的 Widget。
*   **任务3:** 实现选中图层后，动态显示其上下文编辑面板的交互逻辑。
*   **任务4:** 全面应用 Material Design 3 风格到所有UI组件。
*   **目标:** 用户可以使用新的UI完成与旧版相同核心的壁-纸编辑功能，且体验更佳。

### 第三阶段：功能增强 (Phase 3: Feature Enhancement)

*   **任务1:** 实现完整的图层管理功能（添加、删除、复制、调整顺序）。
*   **任务2:** 增加新的图层类型，如 `ImageLayer`（允许用户从相册添加图片）。
*   **任务3:** 引入滤镜或颜色调整功能。
*   **任务4:** 优化导出模块，提供更多导出选项（如格式、分辨率）。
*   **目标:** App 功能更加丰富和强大，成为一款有竞争力的壁-纸创作工具。